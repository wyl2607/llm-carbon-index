# OpenRouter model slug -> energy source + assumptions (Hard Constraint #6:
# ALL model facts live here, never hardcoded in .py).
#
# STATUS: SCHEMA STUB. Seeded with the top ~15 models in Phase 2, each cross-
# checked against EcoLogits / AI Energy Score. The single example below is
# illustrative only and is NOT used by any phase yet.
#
# Per-model schema:
#   energy_source: ai_energy_score | ecologits | fallback
#   params_b:        total parameter count (billions), with source
#   active_params_b: active params for MoE models (billions), with source
#   assumed_hardware: e.g. H100 | A100, with source
#   open_or_closed:  open | closed
#   origin_country:  ISO country of the developing org
#   provider_id:     key into provider_region_map.yaml
#   confidence:      high | medium | low
#   sources:         list of citations for the numbers above

models: {}

# Example (illustrative only — replace with real, sourced entries in Phase 2):
#
# "meta-llama/llama-3.1-70b-instruct":
#   energy_source: ai_energy_score
#   params_b: 70
#   active_params_b: 70
#   assumed_hardware: H100
#   open_or_closed: open
#   origin_country: US
#   provider_id: meta-llama
#   confidence: medium
#   sources:
#     - "Meta Llama 3.1 model card (params)"
#     - "Hugging Face AI Energy Score (Wh/1000 queries)"
